import XCTest
@testable import swift_cyberconnect

final class swift_cyberconnectTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swift_cyberconnect().text, "Hello, World!")
    }
}
